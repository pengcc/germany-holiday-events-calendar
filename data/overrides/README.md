# Reviewed Overrides

Store one YAML override per file. Overrides must record a rationale, evidence URL, scope, creation
time, and review-by date. Generated normalized or published JSON must never be edited directly.

Draft overrides created by the Data Studio remain under `dev_locals/data-runs/` until reviewed and
moved here intentionally.
