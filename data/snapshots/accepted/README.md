# Accepted Raw Snapshots

Raw snapshots are committed only when the source terms explicitly allow redistribution. Otherwise
the repository stores source metadata and content hashes while raw responses remain under
`dev_locals/`.
