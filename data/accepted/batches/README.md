# Accepted Batches

This directory contains normalized source batches written only by `data:publish` after a human
review decision. The files are the versioned source of truth for static deployment data.
