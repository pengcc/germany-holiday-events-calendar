name: to-work-items
description: Convert an approved or sufficiently clear plan into agent-ready vertical work items for focused execution.
category: core
required: true
invocation: user
depends_on:
  - project-memory
version: 0.1.0
triggers:
  - split plan into tasks
  - convert plan to work items
  - create execution steps
  - break feature into vertical slices
  - prepare agent work items
  - slice approved plan
