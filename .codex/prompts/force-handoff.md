# Force Handoff

Use the `handoff` skill.

Create a concise continuation handoff for a future session or another agent.

Save it under `dev_locals/handoffs/`.

Do not duplicate existing artifacts. Reference plans, PRDs, ADRs, issues, commits, diffs, and files by path or URL.

Include suggested skills for the next agent.

Redact secrets and sensitive information.
