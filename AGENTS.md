# Codex Entry Point

Before making changes in this repository, read:

```txt
codex-skills/holiday-sync-germany-project-guideline/SKILL.md
```

Every task must have a saved plan under `dev_locals/plans/` before execution. `dev_locals/` is local working memory and must stay ignored by Git unless explicitly requested otherwise.
